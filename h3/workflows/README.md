# H3 workflows

API-format ComfyUI workflow graphs for MiniMax H3 go here.
The primary programmatic path is `../generate-video.py` (it builds the graph
in code and submits it directly).
If you export graphs from the ComfyUI UI, save them here in API format
(Workflow → Export (API)) for reproducibility.
