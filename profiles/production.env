# Standard Production Environment Profile for Local ComfyUI Music3
COMFY_DIR="${COMFY_DIR:-$(pwd)/../ComfyUI-music3}"
PYTHON_BIN="${PYTHON_BIN:-python3}"
PORT=8188
HOST="0.0.0.0"
EXTRA_FLAGS="--disable-pinned-memory"
COMFY_HOST="http://127.0.0.1:8188"
OUTPUT_DIR="${OUTPUT_DIR:-$COMFY_DIR/output}"
