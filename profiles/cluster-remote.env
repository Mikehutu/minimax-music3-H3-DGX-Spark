# Cluster Remote Client Profile (Execute from orchestrator or worker node)
# Set your target head node or GPU node IP:
TARGET_NODE_IP="${TARGET_NODE_IP:-<NODE_IP>}"
TARGET_PORT="${TARGET_PORT:-8188}"
COMFY_HOST="http://${TARGET_NODE_IP}:${TARGET_PORT}"
