# DGX Spark Co-tenancy Profile (Runs alongside DeepSeek-V4-Flash TP2 on 121GB UMA)
COMFY_DIR="${COMFY_DIR:-$(pwd)/../ComfyUI-music3}"
PYTHON_BIN="${PYTHON_BIN:-python3}"
PORT=8188
HOST="0.0.0.0"
# Co-tenancy flags for GB10 unified memory:
EXTRA_FLAGS="--disable-pinned-memory --disable-smart-memory --disable-cuda-malloc"
COMFY_HOST="http://127.0.0.1:8188"
OUTPUT_DIR="${OUTPUT_DIR:-$COMFY_DIR/output}"
